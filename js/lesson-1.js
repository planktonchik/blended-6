/*"use strict";
const number = prompt("enter number");
if (+number === 10) {
  alert("Вірно ");
}
alert("невірно");
console.log(number); */
const min = Math.floor(Math.random() * (59 - 0) + 0);
if (min >= 0 && min < 15) {
  alert;
}
